<%= form_for(@mostsmallelement) do |f| %>
  <% if @mostsmallelement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mostsmallelement.errors.count, "error") %> prohibited this mostsmallelement from being saved:</h2>

      <ul>
      <% @mostsmallelement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
