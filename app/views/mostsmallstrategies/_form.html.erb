<%= form_for(@mostsmallstrategy) do |f| %>
  <% if @mostsmallstrategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mostsmallstrategy.errors.count, "error") %> prohibited this mostsmallstrategy from being saved:</h2>

      <ul>
      <% @mostsmallstrategy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
