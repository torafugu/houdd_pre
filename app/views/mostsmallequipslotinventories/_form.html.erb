<%= form_for(@mostsmallequipslotinventory) do |f| %>
  <% if @mostsmallequipslotinventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mostsmallequipslotinventory.errors.count, "error") %> prohibited this mostsmallequipslotinventory from being saved:</h2>

      <ul>
      <% @mostsmallequipslotinventory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mostsmallequipslot %><br />
    <%= f.collection_select(:mostsmallequipslot_id, Mostsmallequipslot.find(:all), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :mostsmallmobspecie %><br />
    <%= f.collection_select(:mostsmallmobspecie_id, Mostsmallmobspecie.find(:all), :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
