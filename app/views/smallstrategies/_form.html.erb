<%= form_for(@smallstrategy) do |f| %>
  <% if @smallstrategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smallstrategy.errors.count, "error") %> prohibited this smallstrategy from being saved:</h2>

      <ul>
      <% @smallstrategy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
