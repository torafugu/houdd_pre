<%= form_for(@moretinymap) do |f| %>
  <% if @moretinymap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moretinymap.errors.count, "error") %> prohibited this moretinymap from being saved:</h2>

      <ul>
      <% @moretinymap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.number_field :type %>
  </div>
  <div class="field">
    <%= f.label :map %><br />
    <%= f.text_area :map %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
