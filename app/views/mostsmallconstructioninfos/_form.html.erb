<%= form_for(@mostsmallconstructioninfo) do |f| %>
  <% if @mostsmallconstructioninfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mostsmallconstructioninfo.errors.count, "error") %> prohibited this mostsmallconstructioninfo from being saved:</h2>

      <ul>
      <% @mostsmallconstructioninfo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mostsmallconstructionfamily_id %><br />
    <%= f.collection_select(:mostsmallconstructionfamily_id, Mostsmallconstructionfamily.find(:all), :id, :name) %>

  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :const_period %><br />
    <%= f.number_field :const_period %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
