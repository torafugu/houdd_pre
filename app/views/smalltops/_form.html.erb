<%= form_for(@smalltop) do |f| %>
  <% if @smalltop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smalltop.errors.count, "error") %> prohibited this smalltop from being saved:</h2>

      <ul>
      <% @smalltop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :smalluser %><br />
    <%= f.collection_select(:smalluser_id, Smalluser.find(:all), :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
