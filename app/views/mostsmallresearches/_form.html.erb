<%= form_for(@mostsmallresearch) do |f| %>
  <% if @mostsmallresearch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mostsmallresearch.errors.count, "error") %> prohibited this mostsmallresearch from being saved:</h2>

      <ul>
      <% @mostsmallresearch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
