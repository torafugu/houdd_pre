<%= form_for(@medium_family) do |f| %>
  <% if @medium_family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medium_family.errors.count, "error") %> prohibited this medium_family from being saved:</h2>

      <ul>
      <% @medium_family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ability_sym %><br />
    <%= f.text_field :ability_sym %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
