<%= form_for(@mosttinystrategy) do |f| %>
  <% if @mosttinystrategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mosttinystrategy.errors.count, "error") %> prohibited this mosttinystrategy from being saved:</h2>

      <ul>
      <% @mosttinystrategy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
